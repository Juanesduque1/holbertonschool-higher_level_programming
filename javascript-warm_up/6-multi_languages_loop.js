#!/usr/bin/node
// JS script that prints an array with a loop

const array = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (let i = 0; i < array.length; i++) {
  console.log(array[i]);
}
